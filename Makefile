# $FreeBSD$
#

    COMMENT = Local ports

    SUBDIR += 217
    SUBDIR += homelink
    SUBDIR += icewm
    SUBDIR += joe
    SUBDIR += matlab
    SUBDIR += mlterm
    SUBDIR += net-snmp
    SUBDIR += rxvt-devel
    SUBDIR += shell
    SUBDIR += w3m-config
    SUBDIR += wdm
    SUBDIR += windowmaker

.include <bsd.port.subdir.mk>
